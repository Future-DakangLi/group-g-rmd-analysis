# Results folder

This folder stores generated analysis outputs, including CSV tables, TXT model diagnostics, and the interactive HTML correlation plot.

These files can be regenerated by running `analysis.Rmd`.
